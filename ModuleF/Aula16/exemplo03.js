// também é possível colocar a função dentro de uma variável
// o nome em uma função não é obrigatório

let func = function(x) {
  return 2 * x;
}

// neste caso quem tem nome é a variável que recebe a função anônima

console.log(func(5));